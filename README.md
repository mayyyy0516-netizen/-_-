# 窦桐｜设计师个人作品集

基于 React 18、Vite、Tailwind CSS、Framer Motion 与 React Router 构建的深色科技风设计作品集。

## 本地运行

```bash
pnpm install
pnpm run dev
```

## 生产构建

```bash
pnpm run build
```

构建产物位于 `dist` 目录。Cloudflare Pages 的构建命令填写 `pnpm run build`，输出目录填写 `dist`。

素材替换说明见 `public/assets/README.md`。
